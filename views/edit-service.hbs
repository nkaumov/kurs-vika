<h4 class="center-align">Редактирование услуги</h4>
<form action="/services/edit/{{service.id}}" method="POST" class="col s12 m8 offset-m2">
  <div class="input-field"><input name="name" value="{{service.name}}" required><label class="active">Название</label></div>
  <div class="input-field"><input name="category" value="{{service.category}}" required><label class="active">Категория</label></div>
  <div class="input-field"><textarea name="description" class="materialize-textarea">{{service.description}}</textarea><label class="active">Описание</label></div>
  <div class="input-field"><input type="number" step="0.01" name="price" value="{{service.price}}" required><label class="active">Цена</label></div>
  <button class="btn green" type="submit">Сохранить</button>
</form>
