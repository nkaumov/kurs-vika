<h4 class="center-align">Редактирование мастера</h4>
<form action="/masters/edit/{{master.id}}" method="POST" class="col s12 m8 offset-m2">
  <div class="input-field"><input name="full_name" value="{{master.full_name}}" required><label class="active">ФИО</label></div>
  <div class="input-field"><input name="phone" value="{{master.phone}}" required><label class="active">Телефон</label></div>
  <div class="input-field"><input name="service_category" value="{{master.service_category}}" required><label class="active">Категория</label></div>
  <div class="input-field"><input name="login" value="{{master.login}}" required><label class="active">Логин</label></div>
  <div class="input-field"><input name="password" type="password"><label>Новый пароль (если нужно изменить)</label></div>
  <button class="btn green" type="submit">Сохранить</button>
</form>
